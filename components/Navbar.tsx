import { AppBar, Toolbar, Typography } from "@mui/material";

const Navbar = () => {
  return (
    <div>
      <AppBar>
        <Toolbar>
          <Typography>shopee</Typography>
        </Toolbar>
      </AppBar>
    </div>
  );
};

export default Navbar;
